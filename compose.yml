services:
  web:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - .:/app
    env_file:
      - neco.env
    environment:
      - AI_API_KEY=${OPENAI_API_KEY}
      - AI_BASE_URL=${OPENAI_BASE_URL}
